import 'package:flutter/material.dart';

class AIchatbot extends StatelessWidget {
  const AIchatbot({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Aichatbot"),),
    );
  }
}