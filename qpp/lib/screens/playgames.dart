import 'package:flutter/material.dart';

class playgames extends StatelessWidget {
  const playgames({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
        child:Text("play games")
        
      ),
    );
  }
}