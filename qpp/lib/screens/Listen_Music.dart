import 'package:flutter/material.dart';

class ListenMusic extends StatelessWidget {
  const ListenMusic({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
        child: Text("Listen music"),
      ),
      
    );
  }
}